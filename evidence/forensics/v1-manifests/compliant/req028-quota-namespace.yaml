# TEST: Should be ALLOWED — namespace with ResourceQuota annotation
# Targets: REQ-028 (DORA Art. 9(3)(b))
apiVersion: v1
kind: Namespace
metadata:
  name: test-req028-compliant
  labels:
    dora.test: "true"
    dora.requirement: "REQ-028"
    dora.expected-result: "allowed"
  annotations:
    dora.io/resource-quota-configured: "true"
