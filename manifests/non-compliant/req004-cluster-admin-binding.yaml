# TEST: Should be BLOCKED — binds cluster-admin to a ServiceAccount
# Targets: REQ-004 (DORA Art. 9(4)(b))
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: test-req004-violation
  labels:
    dora.test: "true"
    dora.requirement: "REQ-004"
    dora.expected-result: "denied"
subjects:
  - kind: ServiceAccount
    name: test-sa
    namespace: dora-test
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: cluster-admin
