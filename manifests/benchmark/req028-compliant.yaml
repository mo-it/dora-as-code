# AUTO-GENERATED by scripts/make-benchmark-manifests.py
# replicas set to 0: the object still traverses the full
# admission chain, but schedules no pod, so sync timing
# measures admission rather than container lifecycle.
apiVersion: v1
kind: Namespace
metadata:
  name: test-req028-compliant
  labels:
    dora.test: 'true'
  annotations:
    dora.io/resource-quota-configured: 'true'
