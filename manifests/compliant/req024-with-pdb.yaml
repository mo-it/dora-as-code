# TEST: Should be ALLOWED — critical Deployment WITH PDB annotation
# Targets: REQ-024 (DORA Art. 11(5))
apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-req024-compliant
  namespace: dora-test
  labels:
    dora.test: "true"
    dora.requirement: "REQ-024"
    dora.expected-result: "allowed"
    dora.io/criticality: "high"
  annotations:
    dora.io/pdb-configured: "true"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: test-req024-good
  template:
    metadata:
      labels:
        app: test-req024-good
    spec:
      serviceAccountName: dora-test-sa
      containers:
        - name: pdb-container
          image: nginx:1.27
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            seccompProfile:
              type: RuntimeDefault
